SERVER_PORT=3000
DB_HOST=localhost
DB_PORT=5432
DB_USER=habit_tracker
DB_PASSWORD=habit_tracker
DB_NAME=habit_tracker
DB_SSL=false
JWT_SECRET=super-secret-change-me

# IMPORTANTE:
# Copia este archivo como .env en la carpeta server/ y ajusta los valores según tu configuración.
#
# Para JWT_SECRET: Genera un secreto seguro, por ejemplo: openssl rand -base64 32
# Para DB_*: Usa las credenciales de tu base de datos PostgreSQL local o remota
#
# NOTA: La IP 192.168.0.103 en app.config.js debe coincidir con la IP donde corre este servidor
    